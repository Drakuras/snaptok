{
    "name": "tiktok-downloader",
    "version": "1.0.0",
    "description": "TikTok video downloader — Cloudflare Pages",
    "private": true,
    "scripts": {
        "dev": "npx wrangler pages dev public --port 3000",
        "deploy": "npx wrangler pages deploy public"
    },
    "devDependencies": {
        "wrangler": "^3.99.0"
    }
}